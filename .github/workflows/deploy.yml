name: Production Deploy 🚀

# 1. ТРИГГЕР: Запускаться, когда ты пушишь в ветку main
on:
  push:
    branches: ["main"]

jobs:
  build-and-deploy:
    # 2. АГЕНТ: Самое важное! Говорим работать на ТВОЕМ сервере
    runs-on: self-hosted

    steps:
      # Шаг А: Робот скачивает твой код из GitHub в свою папку
      - name: Checkout Code 🛎️
        uses: actions/checkout@v3

      # Шаг Б: Робот выполняет команды (Имитация деплоя)
      - name: Run Deploy Script 🛠️
        run: |
        echo "🎉 Привет! Я твой Runner."
          echo "Я вижу обновление кода!"
          
          # Показываем, где мы находимся
          echo "Моя рабочая папка: $(pwd)"
          ls -la
          
          # Создадим файл-флаг, чтобы ты мог проверить его наличие на сервере
          touch deploy_success.txt
          echo "Деплой выполнен в $(date)" > deploy_success.txt